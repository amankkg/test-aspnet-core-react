import React from 'react';

export const AboutUs = (props = {}) => {
    return (
        <div>
            <h1>About Us page</h1>
            <p>This is our first custom page in React + ASP.NET Core app</p>
        </div>
    );
}
